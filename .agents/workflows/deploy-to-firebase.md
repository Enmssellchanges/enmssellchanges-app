---
description: Deploy to Firebase Hosting Automatically
---
As explicitly requested by the user, this workflow MUST be run immediately after any code changes are made to the project.

// turbo
1. Execute `npx -y firebase-tools deploy --only hosting` to publish the latest changes.
